[package]
name = "tenant-core"
version = "0.1.0"
edition = "2021"
description = "Core multi-tenancy traits and types — framework agnostic"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
